#clone .git data
git clone --filter=blob:none --no-checkout https://github.com/1abhax/CTF.git writeups
# 啟用只下載指定資料夾模式
git sparse-checkout init --cone

git sparse-checkout set writeups

git checkout main


#update
git status
git add .
git commit -m "Update writeups"
#git pull origin main --rebase
git push origin main